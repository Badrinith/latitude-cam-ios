.DS_Store

# Xcode user state — per-developer, never shared.
# Unanchored so it matches at any depth; the project lives one level down.
xcuserdata/
*.xcuserdatad/
*.xcuserstate

# Build products
build/
Build/
DerivedData/
.build/
*.xcarchive
*.app

.swiftpm/

# Subagent-driven-development scratch: ledgers, briefs, review packages.
.superpowers/
